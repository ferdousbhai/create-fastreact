{
  "name": "{{projectName}}",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm run dev:frontend\" \"pnpm run dev:backend\"",
    "dev:frontend": "cd frontend && pnpm run dev",
    "dev:backend": "cd backend && modal serve modal_app.py",
    "agent": "cd agent && uv run agent",
    "agent:install": "cd agent && uv sync"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
